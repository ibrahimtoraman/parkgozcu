workflows:
  ios-release:
    name: iOS Release
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Get dependencies
        script: flutter pub get
      - name: Build IPA
        script: |
          flutter build ipa --release --no-codesign --project-path ios
    artifacts:
      - build/ios/ipa/*.ipa